npm run build --->create build

git - version controller
github -hosting platform




create repository



git init      -> initialize git repository folder for project (.git)




if not configuired

	git config --global user.email "virajempire2001@gmail.com"

	git config --global user.name "Viraj Vikum"






git add .       -> add all files to staging area(.git folder)


	to add one type files   -> git add *.js
	to add one file   -> git add abc.js
	to remove file    ->git rm --cached abc.js


git status   ---> see changes / identify where we are



git commit -m "commit msg"  ---> create new version of project (by commit by commit)

	using vim editor
		git commit
			To edit -> press i (add msg without #)
			To exit -> press esc , :wq +enter



create .gitignore (in cmd ->   touch .gitignore)

	add files and folders to ignore when commiting
	files-> abc.js
	foders-> /newFoder




create new branch
	git branch login

switch to new branch
	git checkout login (can crete and edit files)



git merge login  -> (merge branch to master branch)



Add repository to a remote repository(GitHub, GitLab)

	git branch -M main   ->Rename branch to "main"



	git remote add origin https://github.com/VirajVikum/vj-vlog-frontend.git

	if change repository
		git remote set-url origin http.....


	git push origin main  (before this create a token)



Add readme file
	create README.md (edit as need)












download git repository


	git clone http/:link..    

	update repo
		git pull














update repository



git add .



git status


git commit -m "second commit"


git push origin master (if this doesn't work)
	git branch
	git push origin brnch_name

