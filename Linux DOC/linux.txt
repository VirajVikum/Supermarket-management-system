pw-115533

sudo su -      				->access root 

mysql -u root -p     			->Access mysql cmd prompt

exit					->exit from mysql



CREATE USER 'viraj'@'localhost' IDENTIFIED BY 'password';




-- Grant all privileges to the user on the specific database
GRANT ALL PRIVILEGES ON call_center.* TO 'viraj'@'localhost' IDENTIFIED BY 'your_password';

-- Flush the privileges to apply the changes
FLUSH PRIVILEGES;




SELECT user, host FROM mysql.user;         ->show all users





sudo systemctl start mysql		->start mysql server

sudo systemctl stop mysql		->stop mysql server

sudo systemctl restart mysql		->restart mysql server

sudo systemctl status mysql		->check status of mysql server







ls                         -> show file lists and directories

pwd                        -> present working directory

cd /                       -> root directory

mkdir newDirectoryName     -> make directory





                  clone github project

git clone https://<token>@github.com/VirajVikum/call-center.git

cd call-center

cp .env.example .env      -> copy .env.example as .env


composer install


php artisan key:generate


npm install

















